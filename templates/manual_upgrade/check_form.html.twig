{% extends '@EasyAdmin/layout.html.twig' %}

{% block page_title %}手动升级检测{% endblock %}

{% block main %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <div class="card mt-4">
                    <div class="card-header">
                        <h3 class="card-title">
                            <i class="fas fa-search"></i> 手动升级检测
                        </h3>
                    </div>
                    <div class="card-body">
                        {% for message in app.flashes('error') %}
                            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                                <i class="fas fa-exclamation-circle"></i> {{ message }}
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        {% endfor %}

                        {% for message in app.flashes('warning') %}
                            <div class="alert alert-warning alert-dismissible fade show" role="alert">
                                <i class="fas fa-exclamation-triangle"></i> {{ message }}
                                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                            </div>
                        {% endfor %}

                        {{ form_start(form) }}

                        <div class="mb-3">
                            {{ form_row(form.distributorId) }}
                        </div>

                        <div class="mb-3">
                            {{ form_row(form.submit) }}
                            <a href="{{ ea_url().setDashboard('App\\Controller\\Admin\\DashboardController') }}" class="btn btn-secondary">
                                取消
                            </a>
                        </div>

                        {{ form_end(form) }}
                    </div>

                    <div class="card-footer text-muted">
                        <h5>使用说明：</h5>
                        <ul>
                            <li>输入分销员ID后点击"检测升级条件"</li>
                            <li>系统将分析该用户是否满足升级条件</li>
                            <li>若满足条件，您可以选择执行升级操作</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
